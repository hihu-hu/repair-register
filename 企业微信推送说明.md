# 企业微信推送说明

这个项目有两种企微推送：

1. 客户每提交一条新登记，马上推送一次。
2. 管理员手动或每天定时推送维修统计。

## 新客户登记自动推送

推送内容包括登记编号、提交时间、快递单号、打印机编号、型号、公司、联系人、电话、故障描述和收件地址。

### 第一次使用要做 3 件事

#### 第 1 件：给数据库加“已推送”记号

1. 打开 Supabase 项目。
2. 点左边的 `SQL Editor`。
3. 新建一条查询。
4. 把 `new-customer-wecom-push-setup.sql` 文件里的全部内容粘贴进去。
5. 点 `Run`。这个文件只需要运行一次。

#### 第 2 件：建立自动推送函数

函数名字必须是：

```text
push-new-customer-submission
```

把 `supabase/functions/push-new-customer-submission/index.ts` 的全部代码放进这个函数，然后部署。

这个函数是给公开的客户登记页调用的，部署时必须关闭 JWT 检查。使用命令部署时写法是：

```text
supabase functions deploy push-new-customer-submission --no-verify-jwt
```

#### 第 3 件：确认企微机器人地址

在 Supabase 的 Edge Functions Secrets 里确认有这一项：

```text
WECOM_WEBHOOK_URL=你的企业微信机器人地址
```

如果原来的“推送企微”按钮能正常发消息，这个地址一般已经配好了，不用重复添加。

### 做完后的效果

客户看到“登记成功”后，企微群会收到一条“有新的客户维修登记”。同一条登记只会推送一次。企微临时失败不会影响客户资料保存，网页会自动重试通知。

## 维修统计推送

### 已经加好的东西

- 页面筛选区右侧新增了“推送企微”按钮。
- 按钮会把这些数据发出去：维修中、今天需要寄、待寄出、返厂中、测试中、未维修。
- 未维修下面会列出客户提交里的快递单号清单，最多显示 30 条。
- 企业微信机器人地址不放在网页代码里，放在 Supabase 的环境变量里，更安全。

### 需要你在 Supabase 后台做的事

1. 打开 Supabase 项目。
2. 进入 Edge Functions，创建并部署 `push-repair-stats`。
3. 把 `supabase/functions/push-repair-stats/index.ts` 里的代码放进去。
4. 给这个 Function 添加环境变量：

```text
WECOM_WEBHOOK_URL=你的企业微信机器人地址
```

5. 部署后，回到网页，管理员登录，再点“推送企微”。

## 大白话提醒

企业微信机器人地址就像群里的发言钥匙，别发给别人，也别放到公开网页代码里。

## 每天自动推送

已经在 Supabase 里加了每天自动推送任务。

- 推送时间：每天早上 9:30（北京时间）
- 定时任务名：`daily-wecom-repair-stats-0930-cn`
- 定时写法：`30 1 * * *`，这是 UTC 时间，换成北京时间就是 9:30
- 定时任务调用的是 `push-repair-stats`，统计数据会从 Supabase 云端数据库重新读取
