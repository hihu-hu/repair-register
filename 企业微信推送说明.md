# 企业微信推送说明

这个功能用来把首页统计数字推送到企业微信群。

## 已经加好的东西

- 页面筛选区右侧新增了“推送企微”按钮。
- 按钮会把这些数据发出去：维修中、今天需要寄、待寄出、返厂中、测试中、未维修。
- 未维修下面会列出客户提交里的快递单号清单，最多显示 30 条。
- 企业微信机器人地址不放在网页代码里，放在 Supabase 的环境变量里，更安全。

## 需要你在 Supabase 后台做的事

1. 打开 Supabase 项目。
2. 进入 Edge Functions，创建并部署 `push-repair-stats`。
3. 把 `supabase/functions/push-repair-stats/index.ts` 里的代码放进去。
4. 给这个 Function 添加环境变量：

```text
WECOM_WEBHOOK_URL=你的企业微信机器人地址
```

5. 部署后，回到网页，管理员登录，再点“推送企微”。

## 大白话提醒

企业微信机器人地址就像群里的发言钥匙，别发给别人，也别放到公开网页代码里。

## 每天自动推送

已经在 Supabase 里加了每天自动推送任务。

- 推送时间：每天早上 9:30（北京时间）
- 定时任务名：`daily-wecom-repair-stats-0930-cn`
- 定时写法：`30 1 * * *`，这是 UTC 时间，换成北京时间就是 9:30
- 定时任务调用的是 `push-repair-stats`，统计数据会从 Supabase 云端数据库重新读取
