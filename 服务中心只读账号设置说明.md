# 服务中心只读账号

网页里登录时填写：

- 账号：`服务中心`
- 密码：`fuwuzhongxin`

第一次使用前，需要在 Supabase 做两件事：

1. 打开 Supabase 项目的 **Authentication → Users**，新增一个邮箱账号：
   - 邮箱：`1041852311+fuwuzhongxin@qq.com`
   - 密码：`fuwuzhongxin`
   - 如果看到“Confirm email / 确认邮箱”，请直接勾选确认。
2. 打开 **SQL Editor**，运行文件 `supabase/migrations/20260918100000_add_service_center_readonly_access.sql` 里的全部内容。

做完后，网页右上角点“账号登录”，输入上面的账号和密码即可。这个账号只能查看“维修记录、客户提交、扫码登记”，不能新增、编辑、删除、确认收货、导入、导出或推送企微。

现在直接打开主网页会先显示登录页；只有登录成功后才会显示系统内容。客户使用的 `customer.html` 扫码登记入口仍然可以直接填写。

如果网页提示“服务中心账号还没有开通”，说明 Supabase 的 Users 里还没有这个邮箱账号，或者密码没有填写成 `fuwuzhongxin`。网页里的登录账号虽然写“服务中心”，但后台必须使用上面的内部邮箱创建用户。
